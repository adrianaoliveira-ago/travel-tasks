import "@testing-library/jest-dom"; // Adds custom Jest matchers for working with DOM elements
// import { server } from "./mocks/server";

// Set up a mock server for API requests
// beforeAll(() => server.listen());
// afterEach(() => server.resetHandlers());
// afterAll(() => server.close());
